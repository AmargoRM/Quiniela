{
  "name": "quiniela-mundial-oficina",
  "version": "1.0.0",
  "private": true,
  "description": "Quiniela estática para fase eliminatoria del Mundial en oficina.",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-worldcup-data.js && node scripts/validate-submissions.js",
    "score": "node scripts/score.js",
    "build": "npm run validate && npm run score",
    "validate:worldcup": "node scripts/validate-worldcup-data.js",
    "test:worldcup": "node scripts/test-worldcup-validation.js"
  }
}
