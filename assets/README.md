# Imagen de la llave

Coloca aquí la imagen real de la llave eliminatoria con el nombre exacto:

```text
assets/bracket.png
```

Este repositorio no versiona binarios. Si `assets/bracket.png` no existe, la web muestra un placeholder visual generado con HTML/CSS y mantiene funcionando el modo de calibración.
